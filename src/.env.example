# Use a keyed raster URL to enable the fast Retina basemap without CARTO's
# "API KEY REQUIRED" watermark. `{ratio}` is replaced with `@2x` at runtime.
VITE_BASEMAP_URL=https://basemaps.cartocdn.com/light_all/{z}/{x}/{y}{ratio}.png?key=your_carto_basemap_key
